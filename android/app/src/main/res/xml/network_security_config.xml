<?xml version="1.0" encoding="utf-8"?>
<!--
  DEV ONLY. Permits cleartext (plain http://) traffic to the local backend during
  development. Everything else stays HTTPS-only, because cleartextTrafficPermitted
  defaults to false outside these specific hosts — so release builds are unaffected.

  If the dev PC's LAN IP changes, add it here (or reserve the IP in the router so it
  doesn't change). 10.0.2.2 is the host as seen from the Android emulator.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">192.168.1.176</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
